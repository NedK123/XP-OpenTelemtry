server:
  port: 8080
spring:
  application:
    name: events-service
  cloud:
    consul:
      host: consul
      port: 8500
management:
  tracing:
    sampling:
      probability: 1.0
  endpoints:
    web:
      exposure:
        include: prometheus

logging:
  pattern:
    level: ${spring.application.name:},%X{traceId:-},%X{spanId:-}